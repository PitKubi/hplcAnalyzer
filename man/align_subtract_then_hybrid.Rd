% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_subtract_then_hybrid.R
\name{align_subtract_then_hybrid}
\alias{align_subtract_then_hybrid}
\title{Align to blank by injector, subtract after injector, then hybrid baseline}
\usage{
align_subtract_then_hybrid(
  df_sample,
  df_blank,
  t_max = 5,
  sg_n = 21,
  sg_p = 2,
  pad_min = 0.2,
  pre_win = 7,
  pre_p = 2,
  noise_factor = 0.1,
  min_peak_dist = 1,
  min_seg_len = 20,
  als_lambda = 7,
  als_p = 5e-04,
  show_plots = FALSE,
  bridge_max_width = 2.5,
  bridge_pad = 0.03,
  bridge_pos_window = 0.8,
  bridge_z_neg = 1,
  bridge_z_pos = 1.2,
  tail_minutes = 5,
  tail_relax = 0.7
)
}
\arguments{
\item{df_sample, data.frame}{Sample trace with columns time,intensity.}

\item{df_blank, data.frame}{Blank trace with columns time,intensity.}

\item{t_max, sg_n, sg_p, pad_min}{Injector finder & padding (min).}

\item{pre_win, pre_p, noise_factor, min_peak_dist, min_seg_len}{Hybrid params for `baseline_hybrid_sm`.}

\item{als_lambda, als_p}{ALS params for the piecewise step.}

\item{show_plots}{Logical; diagnostic plot if TRUE.}

\item{bridge_max_width, bridge_pad}{Width cap and padding for bridge segments (min).}

\item{bridge_pos_window}{Minutes to look back for a positive bump before a negative dip.}

\item{bridge_z_neg, bridge_z_pos}{MAD thresholds for negative/positive peaks.}

\item{tail_minutes, tail_relax}{Relax thresholds in the last minutes of the run.}
}
\value{
list with `df_hybrid`, `shift_min`, `injector`.
}
\description{
Aligns blank to sample by injector midpoint, subtracts only after the
injector window, bridges compact negative peaks (including positive→negative
pairs) in the subtraction, then runs `baseline_hybrid_sm()` on the
post-injector suffix. A 1-minute post-injector guard ramps the ALS baseline
from 0 to avoid dips.
}
