% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate_peak_endpoint_baseline.R
\name{reintegrate_peaks_against_endpoint_baseline}
\alias{reintegrate_peaks_against_endpoint_baseline}
\title{Re-integrate every detected peak against its own endpoint baseline}
\usage{
reintegrate_peaks_against_endpoint_baseline(peak_table, time, signal)
}
\arguments{
\item{peak_table}{Tibble from `detect_peaks_on_smoothed()`.}

\item{time, signal}{The raw trace to integrate.}
}
\value{
A list with the updated `peak_table` and the `geometry` of the tallest peak.
}
\description{
Replaces the `area` column produced by `detect_peaks_on_smoothed()`, and returns the
geometry of the tallest peak so the plot can draw the same baseline it integrated against.
A peak whose envelope cannot be walked keeps the area it already had, so a run never
silently loses a number.
}
