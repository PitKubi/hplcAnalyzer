% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate_peak_endpoint_baseline.R
\name{integrate_peak_against_endpoint_baseline}
\alias{integrate_peak_against_endpoint_baseline}
\title{Integrate a peak against a baseline drawn between its own two feet}
\usage{
integrate_peak_against_endpoint_baseline(
  time,
  signal,
  apex_time,
  n_points = 10,
  neighbour_apex_times = numeric(0)
)
}
\arguments{
\item{time, signal}{Numeric vectors of the trace. Give it the **raw** trace: the chord is the
baseline, so a globally corrected trace would have its baseline removed twice.}

\item{apex_time}{Numeric. Retention time of the peak to integrate.}

\item{n_points}{Integer. Points used to fit each foot level. Default 10.}

\item{neighbour_apex_times}{Numeric. Apexes of the other detected peaks. The envelope stops
at the valley between this peak and the nearest one on each side, so two peaks can never
claim the same stretch of trace and the areas stay comparable to each other, which is what
Area (%) depends on.}
}
\value{
A list with `area`, the two feet `foot_start_rt`/`foot_end_rt` and their fitted
  levels `foot_start_level`/`foot_end_level`, the integrated span `start_rt`/`end_rt`, and
  `drop_rts`, the retention times of the perpendicular drops. `area` is `NA_real_` when the
  peak has no usable envelope.
}
\description{
Integrate a peak against a baseline drawn between its own two feet
}
